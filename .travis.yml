language: haskell
sudo: required

before_install:
# Download and unpack the stack executable
- mkdir -p ~/.local/bin
- export PATH=$HOME/.local/bin:$PATH
- travis_retry curl -L https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
- sudo apt-get update
- sudo add-apt-repository http://apt.llvm.org/trusty/ llvm-toolchain-trusty main -y
- sudo apt-get -y install llvm-3.7

install:
 - stack setup

script:
 - stack test

notifications:
 email:
  on_success: false
